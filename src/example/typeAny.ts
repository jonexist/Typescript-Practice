let sales: number = 100;
let course: string = 'TypeScript';
let isPublished: boolean = true;

const render = (message: string, codeNum: number) => {
  console.log(message, codeNum);
};

render('Hello World!', 200);
